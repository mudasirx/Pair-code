services:
  # WhatsApp pairing website (Node)
  - type: web
    name: whatsapp-bot-pairing
    env: node
    plan: starter
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: SESSIONS_DIR
        value: /var/data/sessions
      - key: LOG_LEVEL
        value: info
    disk:
      name: sessions
      mountPath: /var/data
      sizeGB: 1

  # mysmsportal -> Telegram polling worker (Python)
  - type: worker
    name: mysmsportal-telegram-bot
    runtime: python
    plan: starter
    region: oregon
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: MYSMSPORTAL_USERNAME
        sync: false
      - key: MYSMSPORTAL_PASSWORD
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: POLL_INTERVAL_SECONDS
        value: "30"
      - key: STARTUP_NOTIFY
        value: "true"
      - key: PYTHON_VERSION
        value: "3.11.9"

  - type: worker
    name: mysmsportal-whatsapp-bot
    runtime: node
    plan: starter
    region: oregon
    branch: main
    buildCommand: npm ci --omit=dev
    startCommand: node whatsapp_bot.js
    disk:
      name: whatsapp-auth
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: MYSMSPORTAL_USERNAME
        sync: false
      - key: MYSMSPORTAL_PASSWORD
        sync: false
      - key: WHATSAPP_PHONE_NUMBER
        sync: false
      - key: WHATSAPP_TARGET
        sync: false
      - key: WHATSAPP_AUTH_BASE64
        sync: false
      - key: AUTH_DIR
        value: /var/data/auth_info_baileys
      - key: POLL_INTERVAL_SECONDS
        value: "30"
      - key: STARTUP_NOTIFY
        value: "true"
      - key: NODE_VERSION
        value: "22.12.0"
      - key: NODE_NO_WARNINGS
        value: "1"
